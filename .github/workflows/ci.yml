name: CI Pipeline
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      NEON_DATABASE_URL: ${{ secrets.NEON_DATABASE_URL }} # ✅ Add Database URL
      AUTH_SECRET: ${{ secrets.AUTH_SECRET }} # ✅ Add NextAuth Secret
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install dependencies
        run: npm install

      - name: Debug environment variables  # ✅ Add this step before the build
        run: |
          echo "Database URL: $NEON_DATABASE_URL"
          echo "Auth Secret: $AUTH_SECRET"

      - name: Run lint checks
        run: npm run lint


      - name: Run tests
        run: npm test

      - name: Build Next.js project
        run: npm run build